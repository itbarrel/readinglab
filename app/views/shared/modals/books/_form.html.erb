<div class="modal-header px-card bg-light border-bottom-0">
  <h5 class="modal-title"><%= title %></h5>
  <button class="btn-close me-n1" type="button" data-bs-dismiss="modal" aria-label="Close"></button>
</div>
<%= form_for(book, id: 'addBookModal') do |f| %>
  <div class="modal-body p-card">
    <div class="mb-3">
      <%= f.label :book_name, class: 'fs-0' %>
      <%= f.text_field :name, class: 'form-control', placeholder: 'name' %>
    </div>
    <div class="mb-3">
      <%= f.label :klass,"Class", class: 'fs-0' %>
      <%= f.collection_select :klass_id, current_account.klasses, :id, :name, { include_blank: true }, { class: 'form-control' } %>
    </div>
  </div>
  <div class="card-footer d-flex justify-content-end align-items-center bg-light">
    <%= f.submit 'Save', class: 'btn btn-primary px-4' %>
    <%= f.button 'Cancel', class: 'btn btn-default px-4', type: 'button', data: { 'bs-dismiss': 'modal'} %>
  </div>
<% end %>
