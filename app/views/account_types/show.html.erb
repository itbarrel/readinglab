<p style="color: green"><%= notice %></p>

<%= render @account_type %>

<div>
  <%= link_to "Edit this account type", edit_account_type_path(@account_type) %> |
  <%= link_to "Back to account types", account_types_path %>

  <%= button_to "Destroy this account type", @account_type, method: :delete %>
</div>
