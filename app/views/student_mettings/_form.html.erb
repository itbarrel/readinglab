<%= form_with(model: student_meeting) do |form| %>
  <% if student_meeting.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student_meeting.errors.count, "error") %> prohibited this student_meeting from being saved:</h2>

      <ul>
        <% student_meeting.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
